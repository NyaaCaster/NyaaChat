## 当前版本：v1.2.2
Github Commit: [6fcb748](https://github.com/NyaaCaster/NyaaChat/commit/6fcb748b6e2ca584a3e60489cb2410ef4d5c82bb)

发布日期：2026-06-16

### ✨ 新功能

#### SillyTavern 扩展兼容
- 能够直接运行原版 SillyTavern 的第三方扩展（进行管理员安装，用户不可安装扩展）
- 新增「用户扩展面板」，用户可在界面中查看、启用已安装的扩展（酒馆助手脚本是否可支持未验证）
- 扩展可以读写聊天内容、监听聊天事件，行为与在原版酒馆中一致

#### 前端渲染（HTML/UI 注入）
- 实现了角色卡内嵌前端界面的渲染功能
- 支持原生渲染托管的扩展界面（如 JS-Slash-Runner 一类的扩展 UI）
- 设置界面新增前端渲染开关（注意与酒馆助手的渲染器 `不要同时开` ）

#### 正则脚本管理
- 新增「正则」管理入口，支持全局/角色正则脚本的增删改、启停与排序
- 支持随角色存档的角色正则脚本
- 正则脚本可与 SillyTavern 互相导入、导出（格式兼容）

#### 变量与聊天数据
- 支持按会话保存聊天变量与单条消息级变量，隔离变量避免相互干扰（不再需要mvu和ejs）
- 扩展可对聊天记录进行写回

### 🔧 优化

- 修复从角色卡导入时世界书条目时缺少前端界面条目问题
- 优化导入世界书条目对应角色规则条目转化规则
- 修复窄屏下聊天气泡宽度过窄的显示问题
- 添加角色卡独立旁路请求API，实现新一代模型的agent式调用路径（不再需要副API）
- 完善控制台信息
- 优化版本信息显示
