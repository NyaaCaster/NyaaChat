## 当前版本：v1.6.2
Github Commit: [a62b8ba](https://github.com/NyaaCaster/NyaaChat/commit/a62b8badfe4deea2dcb16b2276d83f5afd6b15bd)

发布日期：2026-09-18

### ✨ 新功能
#### 新增「EJS模板」插件
- 「扩展」中加入「EJS模板」插件
- 替代酒馆「提示词模板」扩展，支持带有 EJS 需求的角色卡在NyaaChat运行
- **优先推荐使用群友开发的[「万象角色引擎」](https://gen.ericfu66.icu/)制作的 EJS 角色卡**
- ⚠️ 需要在「扩展」面板里**手动启用**（插件默认全部停用）
- NyaaChat只负责**运行**角色卡里已有的 EJS 模板，**不提供** EJS 编辑功能

### 🔧 优化
- 「导入角色」的文件选择放宽到 `.png` 与 `.json` 两种格式
    - JSON 卡支持 NyaaChat 原生导出格式，以及酒馆导出的 v1 / v2 / v3 角色卡（世界书、正则、脚本一并导入）
- 发行版不再往浏览器控制台输出 MVU / EJS 的调试信息（这类诊断只在开发实例保留；扩展面板里的运行日志不受影响）

### ⚠️ 重要声明
- EJS 模板可访问本机数据与凭据，**只渲染你信任来源的角色卡**；
- 模板里的死循环会让「发送」流程卡住且无法中断（超过块数／字数上限的条目会被跳过并降级）
- 暂不支持「小手机」脚本，之后会考虑适配
