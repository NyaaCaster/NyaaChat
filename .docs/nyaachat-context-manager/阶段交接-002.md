# NyaaChat 持久化记忆系统 阶段交接 002

> P1 完成交接。P2 待启动。

## 交接目的

续接前必读，按顺序：

1. `开发计划-SSOT.md` — 记忆系统唯一事实来源，§10 阶段划分、§15 详细设计索引
2. `设计-服务端API与数据模型.md` — §2 kind 守卫矩阵（11 处）、§3 记忆库懒创建、§4 完整端点契约
3. `设计-记忆检索实现.md` — fts-query.js 已提取，P2 需要在 searchMemory 基础上做 8 端点
4. `阶段交接-001.md` — 前置项闭环记录
5. 本文件

## 当前进度

### ✅ P1 已完成

| 项 | 状态 | 详情 |
|---|---|---|
| 两仓 Schema 迁移 | ✅ | knowledge: +3 列 (kind, session_id, last_seen_at) + 2 索引；shared-server: +memory_char_max 列 |
| fts-query.js 提取 | ✅ | `services/fts-query.js` 新建，共享 6 常量 + 4 函数 + `sparseSearchTwoPass()`；`retrieval.js` 改为 import |
| ingest 无嵌入配置写入 | ✅ | `services/memory.js` → `ingestMemoryBatch()` 只写 chunks + chunks_fts，不调 embedMany/ensureVecTable |
| 稀疏检索 | ✅ | `searchMemory()` 三表 JOIN session 隔离，复用 `sparseSearchTwoPass()` |
| 路由挂载 | ✅ | `routes/memory.js` 两个端点 (ingest/search) + 一个用量端点 (usage)，`server.js` 挂载 `/memory` |

P1 闸门验证全部通过：
- ✅ 无 embedding config 也能入库并被稀疏检索命中
- ✅ 知识库既有检索行为不回归（fts-query 提取后 KB search 正常）
- ✅ Session 隔离（A 不泄露到 B）
- ✅ 幂等（重复 batch 返回 409）
- ✅ memory_char_max = 2,000,000 默认值

## 本轮已修复 / 已实现

| 文件 | 仓库 | 改动 |
|------|------|------|
| `src/db.js` | knowledge | Schema 迁移块：`knowledge_bases.kind`、`documents.session_id`、`documents.last_seen_at`、`idx_documents_session`、`idx_kb_memory_owner` (partial UNIQUE) |
| `src/server.js` | knowledge | `import { memoryRouter }` + `app.use("/memory", memoryRouter)` |
| `src/services/fts-query.js` | knowledge | **新建**：共享 FTS5 净化模块（6 常量 + `fragments()` + `cjkWindows()` + `fts5Terms()` + `fts5Query()` + `sparseSearchTwoPass()`） |
| `src/services/retrieval.js` | knowledge | 重构：import from `fts-query.js`，稀疏检索块改为 `sparseSearchTwoPass()` 调用 |
| `src/services/memory.js` | knowledge | **新建**：`ensureMemoryKb()` + `ingestMemoryBatch()` + `searchMemory()` + `getMemoryUsage()` |
| `src/routes/memory.js` | knowledge | **新建**：`POST /memory/ingest` + `POST /memory/search` + `GET /memory/usage` |
| `src/db.js` | shared-server | `users.memory_char_max INTEGER NOT NULL DEFAULT 2000000` 迁移 |

### 提交记录

| 仓库 | 提交 | 消息 |
|------|------|------|
| `nyaachat-knowledge` | `bad5baa` | `feat: memory system schema, shared fts-query module, ingest and search` |
| `shared-server` | `b590333` | `feat: add memory_char_max column for persistent memory V1` |

## 仍需继续验证 / 已知问题

| 项 | 说明 |
|---|---|
| 8 端点中仅完成 3 个 | P1 实现了 ingest / search / usage；P2 需完成剩余的 delete session / heartbeat / batches / recompress / delete all，契约见 `设计-服务端API与数据模型.md` §4 |
| kind 守卫矩阵未实施 | `设计-服务端API与数据模型.md` §2 列出的 11 处守卫均未实施——记忆库 id 目前可被所有既有 KB 端点触达（包括 `POST /search` 跨 session 检索） |
| 记忆库懒创建已实现 | `ensureMemoryKb()` 已在 P1 中作为 ingest 的前置实现，P2 无需重做 |
| 前端未改动 | `NyaaChat` 主仓零改动（前端 P3-P5 才涉及） |
| 容器 DB 路径问题 | 当前 `.env` 的 `SHARED_RES_DIR` 和 `KNOWLEDGE_RES_DIR` 指向 WSL 路径，测试时创建的测试 DB 与宿主 `E:\DockerRes` 中的真实数据隔离。这不是 P1 引入的问题，但需要在部署到 macmini 前解决 |

## 续接提示词

```
继续 NyaaChat 持久化记忆系统开发，以 plan 模式推进 P2。

必读：
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\开发计划-SSOT.md（§10 阶段划分、§15 设计索引与契约唯一归属表）
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\设计-服务端API与数据模型.md（§2 kind 守卫矩阵、§4 完整端点契约、§5 前端 API client）
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\设计-记忆检索实现.md（§1 fts-query.js 已提取，§2 searchMemory 已实现）
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\阶段交接-002.md

当前位置：P1 完成。两仓 schema 已迁移（knowledge: bad5baa，shared: b590333），
fts-query.js 已提取，ingest/search/usage 三个记忆端点已上线。
记忆库懒创建 (ensureMemoryKb) 已在 P1 一并实现。

下一步第一件事：按 SSOT §10 的 P2 —— kind 守卫矩阵（11 处逐端点改）+
剩余 5 个记忆端点（DELETE /memory/session/:sessionId、POST /memory/heartbeat、
GET /memory/batches、POST /memory/recompress、DELETE /memory/all）的完整实现。
闸门：记忆库 id 打到任何既有 KB 端点均 404；POST /search 无法绕过 session 隔离。

P2 还需要补齐前端 API client 的函数签名（knowledgeApi.ts 追加 8 个函数），
见 设计-服务端API与数据模型.md §5。

关键约束：
- 全程简体中文
- 改数据库前必须先备份 .db 文件，无例外
- 测试后必须清理所有测试数据和临时脚本
- 禁止未经明确要求启动容器
- 三仓库分开提交推送；git add <file>，禁止 git add -A
- kind 守卫返回 404（不确认记忆库存在），不是 403
- POST /search 的守卫放在 checkKbAccess 内部，不在路由层
- 删除端点双守卫（owner + kind='memory'）
- V1 不写任何 vec_chunks_* 表
```
