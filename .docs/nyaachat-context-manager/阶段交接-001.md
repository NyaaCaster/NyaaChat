# NyaaChat 持久化记忆系统 阶段交接 001

> 本文件记录**前置项闭环 + 设计复核完成**这一轮的交接状态。
> 记忆系统本体（P1~P7）**尚未开工**。
>
> 前置项（`getVecDim` 向量表分区、FTS5 稀疏检索净化）的技术细节、验证结果与
> 真库迁移执行记录，**不在本文件**，一律以两份修复计划为准：
> - `../nyaachat-KnowledgeBase-plan/修复计划-getVecDim跨用户向量表冲突.md`（§5.1 执行结果、§6.1 验证结果）
> - `../nyaachat-KnowledgeBase-plan/修复计划-FTS5稀疏检索静默失效.md`
>
> 本文件只讲"到了哪里、下一步做什么"。

## 交接目的

续接前必读，按顺序：

1. `开发计划-SSOT.md` — 记忆系统唯一事实来源，§10 阶段划分、§15 详细设计索引
2. `复核报告-SSOT完备性.md` — 设计完备性复核结论与复核期间的补充决策
3. `审计报告.md` — 设计依据与 8 条决策
4. 本文件

## 当前进度

### ✅ 前置项已闭环（不再阻塞 P1）

| 项 | 状态 |
|---|---|
| 向量表按维度分区（`vec_chunks_<dim>`，`ensureVecTable` 永不 DROP） | 代码 + 验证 + 两端真库迁移 + macmini 上线，全部完成 |
| FTS5 稀疏检索净化（phrase 转义 + CJK 3 字窗口 + AND→OR 两趟 + catch 打日志） | 同上；镜像 `ab46f3a` 已上线，真实数据实测 CJK 召回 41 行 |
| 提交 | `nyaachat-knowledge` 子仓 `6a45cb3`（修复）、`39f398f`（`.gitignore` 忽略 `package-lock.json`） |

关键结论：两个 owner（`nyaa` / `nyaa4128`）恰好同为 4096 维，`getVecDim` 的 DROP 分支
**从未真正触发** —— 无向量丢失，无需重嵌，无需通知用户重建索引。

### ✅ 设计完备性复核已完成

对 `开发计划-SSOT.md` 做了一轮以"消除执行期现场决策"为目标的复核：

- 补齐 **8 份详细设计文档**（SSOT §15 有完整索引）
- 修正 SSOT 中 5 处事实性错误
- 建立**契约唯一归属表**（SSOT §15），同一契约只有一个权威定义处，其余文档只引用
- 复核期间产生的补充决策全部记入 `复核报告-SSOT完备性.md`

### ⬜ 记忆系统 P1~P7 未开工

阶段划分与闸门条件见 `开发计划-SSOT.md` §10。

## 已知遗留 / 未做

| 项 | 说明 |
|---|---|
| `sparse_top_k` / `dense_top_k` 两列未接线 | `retrieval.js` 候选池深度硬编码 50，与两列默认值一致，故当前零影响；但若将来 PATCH 这两列会得到"改了却无效"的静默假象。用户已决定**维持不改**，记录见 FTS5 修复计划「遗留」段 |
| 重建索引入口缺失 | 用户**主动**更换嵌入模型后，旧维度分区的向量滞留、dense 静默降级为纯稀疏。非本 bug 造成，独立功能缺口，留待单独立项。见 getVecDim 修复计划 §5 第 4 步 |
| 主仓 `.docs/` 未提交 | 8 份设计文档 + 复核报告 + SSOT 更新 + 两份修复计划更新，待用户明确要求后提交 |
| 两端迁移备份未删 | `nyaachat-knowledge-backup-vecpartition-migrate-20260727.db*`（本地另有 `-backup-getvecdim-fix-20260727.db*`），按 MUST 规则保留至确认无副作用 |

## 续接提示词

```
继续 NyaaChat 持久化记忆系统开发，以 plan 模式推进 P1。

必读：
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\开发计划-SSOT.md（§10 阶段划分、§15 设计索引与契约唯一归属表）
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\复核报告-SSOT完备性.md
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\阶段交接-001.md
- P1 的两份依据文档：设计-服务端API与数据模型.md §1、设计-记忆检索实现.md §1

当前位置：前置项（向量表分区 + FTS5 净化）已闭环并上线；设计完备性复核已完成，
8 份详细设计文档就位。记忆系统 P1~P7 全部未开工。

下一步第一件事：按 SSOT §10 的 P1 —— 两仓 schema 迁移 + `fts-query.js` 提取
+ ingest 支持无嵌入配置写入。闸门：无 embedding config 也能入库并被稀疏检索命中，
且知识库既有检索行为不回归。

实现前先核对 SSOT §13「实现前须核对」清单。所有契约以 §15 归属表指定的文档为准，
不要在别处重新定义。

关键约束：
- 全程简体中文
- 改数据库前必须先备份 .db 文件，无例外
- 测试后必须清理所有测试数据和临时脚本，收尾 git status
- 禁止未经明确要求启动容器（docker compose up / run / start）
- 三仓库分开提交推送；git add <file>，禁止 git add -A
- 嵌入模型 apiKey 仅存服务端，不回显明文，不入 settings 导出
- 未经明确请求绝不 commit / push
```
