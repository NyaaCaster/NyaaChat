# NyaaChat 持久化记忆系统 阶段交接 006

> P6 完成交接。P7 待启动（端到端联调 + 部署）。

## 交接目的

续接前必读，按顺序：

1. `开发计划-SSOT.md` — 记忆系统唯一事实来源，§10 阶段划分、§15 设计索引
2. `设计-记忆生命周期与配额.md` — P6 完整设计
3. `阶段交接-005.md` — P5 完成记录
4. 本文件

## 当前进度

### ✅ P6 已完成

| 项 | 状态 | 详情 |
|---|---|---|------|------|
| `RECOMPRESS_SYSTEM_PROMPT` | ✅ | 沿用 `EXTRACTION_SYSTEM_PROMPT` 的防注入声明，任务说明替换为合并规则（5 条） |
| `runExtraction` 可选 prompt | ✅ | 新增 `systemPrompt?: string` 参数，默认 `EXTRACTION_SYSTEM_PROMPT` |
| `selectBatchesToMerge` | ✅ | 选取最老一半批次（≥2 个才返回） |
| `memoryLifecycle.ts` | ✅ | 新建：`maybeHeartbeat()` 函数，1 小时节流 + 500 上限 + 空数组拦截 |
| App 心跳触发 | ✅ | `isLoaded` 后触发初次心跳 + `setInterval` 每 6 小时 |
| ChatHistoryModal 删除清理 | ✅ | `handleDeleteConfirm` 后 fire-and-forget `deleteSessionMemory` |
| 二次压缩流程 | ✅ | 409 → `listMemoryBatches` → `selectBatchesToMerge` → `recompressPrompting` ConfirmDialog → `confirmRecompress`（提取→`recompressMemory`→重试 ingest）→ `cancelRecompress`（丢弃） |
| `recompressMetaRef` | ✅ | 跨 `confirmExtraction`→`confirmRecompress` 传递 batchSeq/taggedMsg |

P6 闸门验证：
- ✅ TypeScript 编译零错误
- ⬜ 空 sessionIds 心跳不发送请求（需 Docker 环境验证）
- ⬜ 删除对话后 `deleteSessionMemory` 被调用（需 Docker 环境验证）
- ⬜ 409 → 二次压缩全链路（需 Docker 环境验证）
- ⬜ 只有 1 个批次时显示"无法合并"消息（需 Docker 环境验证）

## 本轮已修复 / 已实现

| 文件 | 仓库 | 改动 |
|------|------|------|
| `src/lib/memoryExtraction.ts` | 主仓 | 新增 `RECOMPRESS_SYSTEM_PROMPT`(~15行) + `runExtraction` 加 `systemPrompt?` 参数 + 新增 `selectBatchesToMerge`(~6行) |
| `src/lib/memoryLifecycle.ts` | 主仓 | **新建**：`maybeHeartbeat()` 函数，1h 节流 + 500 上限 + 空数组拦截 + fire-and-forget(~40行) |
| `src/App.tsx` | 主仓 | 导入 `maybeHeartbeat`；`isLoaded` useEffect 触发初次心跳 + 6h `setInterval` |
| `src/components/ChatHistoryModal.tsx` | 主仓 | 导入 `deleteSessionMemory` + `useAppSettings`；`handleDeleteConfirm` 追加 fire-and-forget 记忆清理 |
| `src/components/ChatInterface.tsx` | 主仓 | 导入 `RECOMPRESS_SYSTEM_PROMPT`/`selectBatchesToMerge`/`listMemoryBatches`/`recompressMemory`；新增 `recompressMetaRef`；`memory_char_max_reached` 处理改为完整压缩流程；新增 `confirmRecompress`(~100行) + `cancelRecompress` 处理器；新增 `recompressPrompting` ConfirmDialog |

### 提交状态

> 尚未提交 —— 待本交接文档写入后一并 commit push。

## 仍需继续验证 / 已知问题

| 项 | 说明 |
|---|---|
| 端到端闸门未实测 | 心跳、删除对话清理、二次压缩全链路均需 Docker 环境验证 |
| 二次压缩的 `recompressMetaRef` 生命周期 | `confirmRecompress` 成功后清空 ref；`cancelRecompress` 不清空（下次压缩可复用）；正常提取成功路径也会在 ingest 成功后设置 ref（但立即使用，无泄漏） |
| 压缩 ReconfirmDialog 的 budget 估算 | 使用 `usedTokens: null` 构造 budget 传给 `estimateExtractionCost`，估算为 rough 模式，显示为估计值 |

## 续接提示词

```
继续 NyaaChat 持久化记忆系统开发，以 plan 模式推进 P7（端到端联调 + 部署收尾）。

必读：
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\开发计划-SSOT.md（§10 阶段划分、§7 端到端联调）
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\阶段交接-005.md
- H:\GitHub\NyaaChat\.docs\nyaachat-context-manager\阶段交接-006.md

当前位置：P6 完成。V1 全部代码开发阶段（P1-P6）已结束。

P7 待做事项：
1. rebuild 前端 + knowledge 镜像
2. 部署到 macmini（scp + restart.py）
3. E2E 验证：开关启用 → 长对话 → 自动提炼 → 线索标记 → 检索召回 → 删除对话 → 记忆归零
4. 验证不变量：memory_context 不进 system、session 隔离、开关默认关闭、锚点三类声明
5. 如发现 bug 就地修复并记录

关键约束：
- 全程简体中文
- git add <file>，禁止 git add -A
- 使用 H:\GitHub\passwds.md 中的 GitHub PAT 做 git push
- 部署前先 stop Windows 容器（迁移前置条件）
- 部署到 macmini: scp compose + restart.py + .env → ssh restart
```
