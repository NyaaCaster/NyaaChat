# NyaaChat V4 ComfyUI-Fixed T2I Agent 阶段交接 001

## 交接目的

本文件记录 P1 阶段完成状态，供后续开发（P2）或新对话接续使用。

**续接前必读**：
- `V1初始设计.md` — 初始设计意图
- `设计决策.md` — 关键决策记录
- `开发计划-SSOT.md` — SSOT 计划（P1 已标记 ✅）

## 当前进度

### ✅ P0 — 三路径脚手架拆分（已完成，本阶段前）

`handleGenerateImage` 拆为 isComfyPack / isComfyImage / else 三分支；`buildFixedComfyPrompt` + `buildFixedComfyPromptRequest` 脚手架就位。

### ✅ P1 — ext-host 服务端代理端点（本阶段完成）

ext-host 新增 LLM 代理端点 `POST /t2i-agent/chat`，部署方密钥仅存服务端 `process.env`，验证全部通过。

## 本轮已实现

| 文件 | 改动 |
|------|------|
| `ext-host/src/server.js` | 新增 `proxyT2iAgent()` 函数（122-168）+ 常量 `T2I_AGENT_ENV_KEYS` + 路由注册 `POST /t2i-agent/chat`（277-279）。照抄 `proxyTts` 骨架：校验 env → 读 messages → 组装 `{model, messages, stream:false}` → fetch deepseek `/v1/chat/completions` → 回传 JSON。超时默认 120s。 |
| `docker-compose.yml` | ext-host 加 `env_file: - .env`，使 `COMFYUI_FIXED_T2I_AGENT_*` 变量进入容器。 |
| `nginx.conf` | 新增 exact-match location `= /api/ext-host/t2i-agent/chat`（185-197），300s 超时、buffering off。优先于通用 `/api/ext-host/` prefix。 |
| `.env.example` | 补充 4 项 COMFYUI_FIXED_T2I_AGENT_* 变量模板及注释。 |

## 已验证

1. ✅ curl 正常调用 → deepseek 返回 `chat.completion` JSON
2. ✅ body 传 `model: "gpt-5-turbo"` → 被忽略，实际使用 env 中 `deepseek-v4-flash`
3. ✅ 响应中无 key/baseURL 泄漏
4. ✅ `server.js` 中仅 `process.env` 键名字符串引用，无硬编码密钥
5. ✅ rebuild → ext-host + app 容器正常运行

## 仍需验证 / 已知问题

无。P1 为纯服务端基础设施，已通过 curl 验证全路径。用户无需手动测试。

## 续接提示词

```
继续开发 NyaaChat 的 ComfyUI-Fixed T2I Agent 功能，进入 P2 阶段。

必读文档（均在 NyaaChat\.docs\comfyui-fixed-t2i-agent\ 下）：
- V1初始设计.md — 初始设计意图（画图提示词 agent 的核心逻辑）
- 设计决策.md — 关键决策（单阶段 LLM、软字数控制、服务端 model 强制）
- 开发计划-SSOT.md — SSOT 计划，P1 已完成

当前进度：
- P0 三路径脚手架拆分 ✅
- P1 ext-host 代理端点 ✅（POST /api/ext-host/t2i-agent/chat 已上线，密钥安全）

P2 任务：Agent 提示词工程
- 重写 buildFixedComfyPromptRequest（src/lib/chatPipeline.ts），产出 {system, user}
- system prompt 需覆盖 7 维度结构分析框架 + 分段要求 + 软字数（~360词/6段）+ 纯英文
- user message 组装角色 description + 用户 profile + 最近 N 轮 + 焦点消息
- 参考 TestPrompt.md、.ref/TestPrompt的结构化拆分.json、.ref/一个结构化的画图提示词样例.md

关键约束：
- 软字数控制：作为提示词文本注入，禁止 slice LLM 输出
- model 由服务端 env 强制，前端不传 model/key/baseURL
- 三仓库分开提交（主仓 / knowledge / shared），P2 只涉及主仓
- rebuild 用 python rebuild.py

请以 plan 模式推进 P2。
```
