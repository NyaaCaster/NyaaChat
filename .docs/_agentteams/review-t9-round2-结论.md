# 复审记录 t11（第 2 轮）：t9 是否关闭 F1–F8 且未过度修复

> 复审对象：`H:\GitHub\NyaaChat\.docs\AnswererFlagalac-可行性考察汇总.md`（904 行 / 134,455 字节 / mtime 2026-09-12 00:43:50）
> 被复审任务：**t9**（repair，承接 F1–F8；sourceFindingIds=F1–F8）
> 第 1 轮复审：t6（`review-t5-t6-bypass-options.md`）→ needs_revision，产出 F1–F8
> 复审人：`bypass-options`（Q3 报告 v3 作者、F1–F8 提出者）
> 约束遵守：只读复审 —— 未修改被复审文档、未修改任何业务源码；本记录为唯一写入物
> **结论：verdict = pass（8/8 findings 全部关闭，无过度修复，无新增口径矛盾，完整性通过）**

---

## 1. 独立复跑的 5 组 verify 实测输出（自己执行，未采信自报）

```
PS> Select-String -Path '.docs\AnswererFlagalac-可行性考察汇总.md' -Pattern 'E1-first','K1','V11','V12','可选增强','M1','M5' | Group-Object Pattern | Select-Object Name,Count

Name      Count
----      -----
可选增强      2
E1-first    26
K1          12
M1          13
M5           7
V11         12
V12          2

PS> ... -Pattern '无 absolute','无 depth','无该通道','无此通道','无绝对注入'
hits=0

PS> ... -Pattern 'P4→L2 与 P37→L1'
hits=0

PS> ... -Pattern 'identityCard.*（L1）','dualModelCard.*（L1）'
hits=0

PS> git status --short
 M src/App.tsx
 M src/components/BypassModal.tsx
 M src/lib/settingsBackup.ts
 M src/types.ts
?? ".docs/AnswererFlagalac-可行性考察汇总.md"
?? .docs/_agentteams/
?? src/lib/FlagalacTemplates.ts
```

对照说明：
- **V1** 与 captain 转达的 t7 实测（E1-first 26 / K1 12 / V11 12 / V12 2 / 可选增强 2 / M1 13 / M5 7）**逐项一致**；与第 1 轮（24/12/10/2/2/13/7）相比 E1-first +2、V11 +2，与本次新增的落位细分表述相符。
- **V4 = 0 命中**符合验收口径（现役结论处期望 0）。历史记录段落用的是「落 L1 / 放 L1 / 又被写成 L1」写法，不含「（L1）」，故不被该正则命中；已逐行人工确认（见 §3）。
- `src/` 5 项 mtime 全为 **2026/9/11 23:33**（早于本轮），git status 仅 `.docs/` 新增 ⇒ **未修改业务源码**。

---

## 2. F1–F8 逐条关闭确认（改后原文片段 + 行号）

| # | 原 finding（t6） | 改后原文片段（行号） | 判定 |
|---|---|---|---|
| **F1** [high] | §7 P2「C2 注入逻辑第一批」误把 `identityCard`/`dualModelCard` 标为 **L1** | **L545**：`注入逻辑第一批（确定能做的）：**E1 尾 system 独立新块**（共用 **M1 一处改造**）承载 5 个注入类载荷 —— identityCard / dualModelCard / refusalImmunity / disclaimerSpell / unicodeEncoding；traceCleanup（**L5 正则层，零改造**）。**注意：L1「通道保真模式」不在首版，须先过 V10/V11（§12.4）**`（原「（L1）」已删） | **passed** |
| **F2** [medium] | §7 A1 理由仍是「整个落位映射都建立在它之上」；导语仍称 A1 与 A5 并列最高优先 | **L517**（导语）：`**A5（落位基线确认）为最高优先**；**A1（Gemini 兼容层行为）只影响 L1 可选退路是否有收益、不阻塞首版**（仍建议尽早实测）`；**L523**（A1 理由列）：`**本项只决定 L1 可选退路（V10/V11）是否有收益，E1-first 首版不依赖它**；仍建议尽早实测以决定是否启用 L1` | **passed** |
| **F3** [medium] | §2 术语表「绝对注入 depth=0」仍写「NyaaChat **无此通道**」 | **L85**：`NyaaChat 装配层没有该语义；ST 兼容层 setExtensionPrompt（compat/stContext.ts:67）**有注册位**但 getExtensionPrompts（:79）无消费点 ⇒ 现成未接线、需小–中改造；即便接线也只能并入 E1 新块` | **passed** |
| **F4** [medium] | §5.8 C6 未同步 Q3 v3（缺「位置类=有注册位无消费点」与 `api.ts:859 max_tokens:4096`） | **L444**：`NyaaChat 缺 **request-side 能力**：无 prefill、无 stop（Q2 修正：位置类不是「无通道」，而是**有注册位无消费点** —— compat/stContext.ts:67 注册 / :79 无消费点）、Anthropic max_tokens 硬编码 **4096**（api.ts:859）` + `先补 api.ts 发送层能力（stop 等）再补 UI；位置类一律并入 E1 新块（M3）` | **passed** |
| **F5** [low] | §5.8 C7 仍是 v1 措辞（「P37 载荷放 L1 时…」） | **L445**：`落静态前缀的载荷（**仅 L1 可选退路**）会**击穿 prompt cache**，须 UI 明示；**首版 E1 只改断点②之后的尾部字节，不受影响**` + `**禁止**把每轮变化内容…塞进 L1；L1 须先过 **V10/V11**` | **passed** |
| **F6** [low] | §6.1 第 2 条「这与 Q3 把 identityCard 放 L1（静态前缀）不冲突」 | **L490**：`…**这与 Q3 v3 的 E1-first 基线不冲突（锚点句恒定 vs 载荷随配置变）；若载荷改走 L1，则触发 §12.6/K1 的缓存代价，须先过 V10/V11。**` | **passed** |
| **F7** [low] | §2 术语表交叉引用「见 §8 M4」（M4 实为 §9；§8 对应 U1） | **L84**：`**未实测**，见 §8 U1 / §9 M4（代码注释 api.ts:526-534 + 标准 §5 表格 line 105）` | **passed** |
| **F8** [low] | §12.7 首行称 §5.5/§5.2「全部标注 E1」，与 L4/L5/L6 分层不符 | **L897**：`7 个载荷的默认落位 ｜ **5 个注入类载荷（identityCard/dualModelCard/refusalImmunity/disclaimerSpell/unicodeEncoding）+ traceCleanup 的规则文本标注 E1**；toolChannel = L4、traceCleanup = L5、unicodeEncoding 另含 L6 解码（L1 明确「默认不用」） ｜ §12.2：**全部 7 个载荷默认落 E1**，并注明「落位细分见 §5.5：toolChannel 实走 L4、traceCleanup 实走 L5」 ｜ ✅ 一致` | **passed** |

**F8 的交叉引用已回源验证**：该行声称 §12.2 有细分注释 —— **L828 确实存在**：`**收敛决定**：全部 7 个载荷（**含 dualModelCard**）默认落 **E1**（= Q3 的 **L2**；**落位细分见 §5.5**：toolChannel 实走 L4、traceCleanup 实走 L5）…`。且该行第一列所描述的 §5.5 —— **L353 确实已改写**为「5 个注入类载荷 + traceCleanup 的规则文本落 E1；toolChannel = L4、traceCleanup = L5、unicodeEncoding 另含 L6 解码」。⇒ 交叉自检表两侧与实体表**三处一致**。

**8/8 = passed**。

---

## 3. 反向检查（防过度修复）：v1 争议记录必须仍在 ✅

| 要求保留的位置 | 现状 | 判定 |
|---|---|---|
| §9 **M9**（Q3 内部落位自相矛盾） | **L674** 标题仍在：`### M9｜Q3 报告内部的落位自相矛盾：§5.2 把 identityCard 放 L1，§6.1 却说「P4 目标载荷放 L2」`（含 v1 原文引用与「✅ v3 已自行修正、本项结案」两段） | **保留** ✅ |
| §9 **9.10**（版本演进 v1→v2→v3 + K1） | **L710** 标题仍在；**L720** 的 K1 记录全文仍在（含「v1 基于通道保真度建议 P37 的 dualModelCard 落 L1 静态前缀」） | **保留** ✅ |
| §12 **12.1**（K1 冲突记录） | **L820** 标题 `### 12.1 冲突记录（K1）：v1 的「P37 → 静态前缀」vs Q1 硬约束 3`；**L822** `**v1 的主张**：gemini37flash（P37）的载荷落 **L1 静态前缀**…同一目标在 v1 的 §5.2 里 identityCard 又被写成 L1…（§9 M9）` | **保留** ✅ |
| 其他历史留档 | **L70**（§1 必读表：Q3 v1 曾建议…）、**L297**（§5.2 `dualModelCard` 行括注「v1 曾建议 L1 静态前缀，v2 因 K1 收敛」）、**L361**（§5.5「原始观察（保留作保真度依据）…P37 载荷「应」落 L1」） | **保留** ✅ |

**无一处被清理** ⇒ 不构成过度修复。

---

## 4. L331 / L502「没有通道」类写法复核

- **L331（§5.4(a) 第 3 点）**：已是更正后的表述 —— `v3 按 Q2 修正为 —— setExtensionPrompt（compat/stContext.ts:67）有注册位，但 getExtensionPrompts（:79）全仓无消费点 ⇒ 属「现成未接线、需小–中改造」；且即便接线，按 Q1 也只能并入 E1 新块，不能新增第二条尾部 system` ⇒ **该处指「位置类注入」，已更正** ✅
- **L502（§6.2 第 4 行）**：已是更正后的表述 —— `v3 更正表述：setExtensionPrompt（compat/stContext.ts:67）有注册位，但 getExtensionPrompts（:79）全仓无消费点 ⇒ 现成未接线、需小–中改造；且即便接线也只能并入 E1 新块` ⇒ **同属位置类，已更正** ✅
- 现存「没有该语义」类写法仅 **L85 / L122** 两处：`NyaaChat 装配层没有该语义` / `**装配层没有 depth/absolute 语义**`。二者指的是 **`ApiMessage[]` 一次性拼装这一装配模型本身不具备 depth/absolute 语法**（属**真实能力缺失**），且**紧接其后就是 Q2 更正**（有注册位无消费点）。⇒ **保留理由成立**，不构成「把位置类注入说成不存在」✅

---

## 5. 落位口径一致性（§1 / §5.2 / §5.5 / §5.9 / §6.1 / §7 / §12 七处）

| 位置 | 现役口径 | 判定 |
|---|---|---|
| §1 速览 Q3 段 **L57** | `7 个载荷默认全部落…新块…（= Q3 的 L2 = Q1 的 E1）`，并细分 `5 个注入类载荷 + traceCleanup 规则文本 → E1；traceCleanup 脚本 → L5；toolChannel → L4、unicodeEncoding 另含 L6；L1 不在首版，须先过 V10/V11` | ✅ |
| §5.2 七开关表 **L294–L300** | 5 项落 **E1**（L1 仅可选增强、须 V10/V11）；`toolChannel` = L4 依赖改造；`traceCleanup` = L5 正则层；`unicodeEncoding` = E1 + L6 解码 | ✅ |
| §5.5 落位表 **L352–L353** | L1 = 默认不用/可选增强/须 V10+V11；L2 = E1 且细分 `toolChannel→L4、traceCleanup→L5、unicodeEncoding 另含 L6` | ✅ |
| §5.9 落地顺序 **L453** | `P3 M1 + M4`（M1：1 处改造供 5 个选项共用）；L1 通道保真模式仅在 V10/V11 通过后另立阶段 | ✅ |
| §6.1 推导链 **L490** | 「不冲突…若载荷改走 L1，则触发 §12.6/K1 的缓存代价，须先过 V10/V11」 | ✅ |
| §7 前置条件 **L517 / L523 / L545** | 导语 A5 最高优先、A1 不阻塞首版；A1 理由只影响 L1；C2 = 5 项共用 M1 落 E1 + L1 不在首版 | ✅ |
| §12 **L828 / L858 / L897** | 12.2 收敛决定（含细分注释）、12.4 门槛、12.7 交叉自检 | ✅ |

**七处互相不冲突**，均为「E1 为默认通道 + 个别项实际落 L4/L5/L6 + L1 默认关且须门槛」的同一口径。

---

## 6. 完整性

| 项 | 实测 | 判定 |
|---|---|---|
| R1 / R2 章节 | `### R1（最高优先）｜…`（L744）、`### R2｜同一 identifier…`（L783）均在，另有 R3–R6 表（L798） | ✅ |
| 前置条件 | P0 **5**（A1–A5）+ P1 **6**（B1–B6）+ P2 **4**（C1–C4）+ P3 **5**（D1–D5）= **20 条**；P4「明确不做」8 项另列 | ✅ |
| 待验证项 | A 组 **9**（U1–U7、U20、U21）+ B 组 **7**（U8–U13、U22）+ C 组 **6**（U14–U19）= **22 条** | ✅ |
| §9 矛盾/证据分层 | **M1–M10 共 10 节**齐全（M1 L616 … M10 L683），另 §9.9 复核表（L689）、§9.10 版本演进（L710） | ✅ |
| 未修改业务源码 | git status 仅 `.docs/`；`src/` 5 项 mtime 均 2026/9/11 23:33 | ✅ |

---

## 7. 非阻塞观察（不构成 finding）

1. **§5.4(a) 标题（L324）仍为「当前无通道 / 未实现（做不出来，或做出来会骗人）」**，而其下 L328–L331 已逐条更正（停止串全局化、预填为改造项、位置类有注册位无消费点）。这与 **Q3 v3 §5.3(a) 的同一现象**一致，且 §9 M3（L633）已把它登记为「标题与内容措辞不完全一致的表述问题，不阻塞开发」。⇒ 保留可接受。
2. **Q3 v3 §6.1 的 L2 行仍写「全部 7 个载荷默认都落这里」**（Q3 文件，本次复审对象之外；起草者为我本人）。本轮 F8 已在汇总文档侧精确化；建议后续把 Q3 同一措辞一并收敛（可另行处理，**不影响本 verdict**）。
3. §12.2 的「全部 7 个载荷默认落 E1」为简写，**同句内已带细分注释**，且 §12.7 已精确化 ⇒ 可接受。

---

## 8. 声明

本次为**只读复审**：未修改被复审文档 `AnswererFlagalac-可行性考察汇总.md`，未修改 `src/` 下任何业务源码（见 §1 git status 与 mtime），唯一写入物是本复审记录。t9 的修复为纯文档编辑，未引入代码改动；未发现过度修复（v1 争议记录全部保留）；未发现新增口径矛盾。

---

## 9. 附记（out-of-band 编辑复核）：以**冻结版**重新取样后的结论 —— **verdict 维持 pass**

### 9.1 out-of-band 编辑的事实记录

| 项 | 值 |
|---|---|
| 时间 | **2026/09/12 00:45:10.946** |
| 作者 | `prompt-critic`（t7 被转交后所为；已由 captain 打断其回合） |
| 范围 | **仅末尾增量两行**：新增 L902（一条 blockquote「上表第 1 行的措辞与 Q3 v3 的关系（记录一条跨文档措辞收敛建议）」）+ L903（空行）；原产出说明行由 L904 位移到 **L906** |
| 行/字节 | 904 行 → **906 行**；非空 665 → **666**；134,455 B → **135,556 B** |
| 我此前的 t11 取样 | mtime **00:43:50**、904 行（**已被冻结版取代**，本附记即对冻结版的重新取证） |
| 并发写检查 | 取样前后两次 `Get-Item` 的 mtime **完全相同**（00:45:10.946）⇒ 复核期间**无并发写入**，文件可审 |

### 9.2 冻结版五组 verify 实测（重新取数，未采信 captain 代跑值）

```
SAMPLE-1 mtime=2026/09/12 00:45:10.946 bytes=135556 lines=906
SAMPLE-2 mtime=2026/09/12 00:45:10.946 bytes=135556 lines=906   ← 两次一致，无并发写
V1 七记号：可选增强=2、E1-first=26、K1=12、M1=13、M5=7、V11=12、V12=2
V2 五词（无 absolute|无 depth|无该通道|无此通道|无绝对注入）= 0
V3 'P4→L2 与 P37→L1' = 0
V4 'identityCard.*（L1）|dualModelCard.*（L1）' = 0
V5 git status：仅 .docs/ 新增；src/ 4 个 M + 1 个 ?? 仍为 2026/9/11 23:33 既有改动
lines=906 nonEmpty=666
```

与 captain 代跑值**逐项一致**，与我上一轮 t11 的 904 行取样也**逐项一致**（增量两行不含任何记号）；与 t7 转达值亦一致。

### 9.3 增量两行的内容核验（是否引入新矛盾）

- **L902**：内容为「§12.7 上表第 1 行的精确写法源自本文对 Q3 v3 §6.1 的收敛，而 Q3 v3 §6.1 自身仍写作『全部 7 个载荷默认都落这里（L2）』——属两文档共享的措辞不精确；建议 Q3 v3 §6.1 采用相同措辞；**该收敛需修改来源报告 Q3，不在本次汇总修复的 inScope 内，可另行处理，不阻塞本修复**」。
  - **事实核验**：Q3 v3 §6.1 的 L2 行确实仍为宽表述 ⇒ **陈述为真**（这正是我 t11 的「非阻塞观察 ②」所指，双方一致）。
  - **与 §12.7 的关系**：是对该表第 1 行的**说明性附注**，不改变表内结论，**不构成冲突**。
  - **与 §9 M9 的关系**：M9 处理的是 **v1 的内部落位自相矛盾（`identityCard` 放 L1 vs「P4 载荷放 L2」）**，已于 v3 结案；L902 处理的是 **Q3 v3 §6.1 的行文精度**（非矛盾类）。**主题不同，不重复、不冲突**。
  - **唯一小瑕疵（trivial，不影响验收）**：L902 把该精确措辞的来源归为「本文对 Q3 v3 §6.1 的收敛」，省略了它实际来自 t6 的 finding F8 → t9 修复这一链路；属表述简化，**不构成事实错误**（该写法确实由本文采纳并落在此处）。
- **L906**：产出说明/免责行（内容与位移前一致）⇒ **无新增语义**。
- **对 F1–F8 关闭结论的影响**：**零**。八处锚点在冻结版仍在同一行号（L84 / L85 / L444 / L445 / L490 / L523 / L545 / L897），我已重新逐条定位确认。
- **对反向防过度修复检查的影响**：**零**。§9 M9（L674）、§9.10（L710）、§12.1（L820、L822）仍在，无清理。

### 9.4 L331 / L502 与「真实能力缺失陈述」的复核（captain 第 4 点）

- **L331（§5.4(a) 第 3 点）与 L502（§6.2 第 4 行）**：均已属**更正后的位置类表述**（有注册位 `compat/stContext.ts:67`、无消费点 `:79` ⇒ 现成未接线、需小–中改造；即便接线也只能并入 E1）⇒ 与 synthesizer 的「两处均为更正」判定**一致**。
- **保留的「真实能力缺失」陈述**已逐处核验，**均非位置类**且措辞正确：
  - **无 `stop`**：L126（B7：请求体 `api.ts:382-392` / `:857-881` 无 `stop`/`stop_sequences`；补字段属「发送层新增能力」；并注明全局副作用与「原版本就没生效」）、L444、L501；
  - **无 prefill**：L223（`assistant_prefill` 类续写锚点「无 prefill 概念」）、L444、L501；
  - **无原生 reasoning**：L306、L463（`REASONING=6` 零调用点、流解析不读 `reasoning_content`）、§1 硬性前提 ③。
  ⇒ **该判定成立**：位置类（有注册位无消费点）已更正，prefill/stop/原生 reasoning 属真实能力缺失，保留合理。

### 9.5 附记结论

- out-of-band 编辑**属增量说明**（末尾两行），**未损害可审性**（无并发写、无越界改动、不触及 F1–F8 与任何验收所依赖的章节）；
- **未引入新的口径矛盾**；L902 与 §12.7 / §9 M9 不冲突、不重复；
- ⇒ **冻结版（mtime 2026/09/12 00:45:10.946，906 行 / 非空 666）的复审结论与 §2–§6 相同：F1–F8 全关闭、验收全通过、verdict = pass。**
- 注：t11 任务已为终态（completed / verdict=pass），其结果不可再更新；本附记即为**针对冻结版**的补充取证。若质量门要求「对冻结版正式登记一个通过复审」，请另开一个复核任务（同一验收清单，我可以直接复用本附记的取数并复跑）。
