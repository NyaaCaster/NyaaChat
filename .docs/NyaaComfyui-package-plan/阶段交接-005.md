# NyaaComfyui图包 V1 阶段交接 005

## 交接目的

P5 完成，为 P6（对话生图链路 gate + 扣减）提供续接入口。

**续接前必读**：
- SSOT（P1-P5 ✅）
- `阶段交接-004.md` — P4 API 参考
- `NyaaComfyui图包-初始设计.md` 第12行 — 计费边界

## 当前进度

P1-P5 ✅ — P5 ComfyUI 节点 UI 完成：

- **启用开关 gate**：`comfyui-fixed`/`comfyui-custom` 从关→开时检查登录态，未登录打开 `UserAccountModal`，不切换开关
- **图包剩余显示**：连通性检查后方新增 Field，登录后显示 `剩余 N 次` + 扩容按钮；未登录显示「登录后可见」
- **扩容按钮**：复用 ConfirmDialog，确认后调 `expandComfyuiPack`，刷新 account profile
- `npm run build` 通过
- commit `6b14f7c` 已推送

## 本轮改动

| 文件 | 改动 |
|------|------|
| `src/components/ImageProvidersModal.tsx` | 导入 PackagePlus/UserAccountModal/API；ImageProviderDetailProps 扩展；account 状态+UserAccountModal；DetailHeader onBeforeEnable gate；ComfyProviderDetail 图包剩余 Field+扩容 handler+ConfirmDialog |

## P6 开发参考

P6 在 `ChatInterface.tsx` 的对话生图链路中：
- `handleGenerateImage` / `runImageGeneration` 分流增强（仅 `isComfyImage`）
- 登录 gate → 未登录打开 UserAccountModal
- 剩余预检 → `remaining <= 0` 弹窗「NyaaComfyui图包 剩余次数不足，是否要扩容？」
- 成功后调 `consumeComfyuiPack(token)` −1
- 对标代码：`ChatInterface.tsx:885-1067`（runImageGeneration）、`:1113-1171`（handleGenerateImage）

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1-P5 ✅（后端+数据层+账号面板+节点UI 全部完成并推送）。

请以 plan 模式推进 P6 — 对话生图链路 gate + 扣减：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT §6.3）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-005.md（本文件）
3. P6 任务：ChatInterface.tsx 对话生图链路加登录gate、剩余预检、成功后 consumeComfyuiPack −1、耗尽弹窗
4. 仅 isComfyImage（comfyui-fixed/custom）触发；其他供应商走现有流程
5. 验证：npm run build + 如需真机验证
6. 遵循 NyaaChat CLAUDE.md 和 Vibo Coding 规范
```
