# NyaaComfyui图包 V1 阶段交接 006

## 交接目的

P6 完成，为 P7（端到端联调 + rebuild 真机验证）提供续接入口。

**续接前必读**：
- SSOT（P1-P6 ✅）
- `阶段交接-005.md` — P5 ComfyUI 节点 UI 参考
- `NyaaComfyui图包-初始设计.md` 第12行 — 计费边界

## 当前进度

P1-P6 ✅ — P6 对话生图链路 gate + 扣减完成：

- **登录 gate**：`handleGenerateImage` + `handleRegenerateImage` ComfyUI 路径在生图前检查登录态，未登录打开 `UserAccountModal` 并中止
- **剩余预检**：已登录时检查 `comfyuiPackRemaining`，`<= 0` 弹出耗尽 ConfirmDialog（取消关闭 / 扩容打开账号面板）
- **成功后扣减**：`runImageGeneration` ComfyUI 成功分支 fire-and-forget 调 `consumeComfyuiPack` −1
- **gate 时机**：在 `handleGenerateImage` 中 gate 检查在 LLM 英文提示词构建之前，避免浪费 LLM 调用
- `npm run build` 通过
- 非 ComfyUI 供应商（QinyAPI / OpenAI 兼容）流程不受影响

## 本轮改动

| 文件 | 改动 |
|------|------|
| `src/components/ChatInterface.tsx` | +73/-3 行：imports 扩展（consumeComfyuiPack/StoredAccount/UserAccountModal/ConfirmDialog）；新增 isAccountOpen/showExhaustedDialog state；新增 checkComfyGate() 辅助函数；handleGenerateImage ComfyUI 分支加 gate（LLM 前）；handleRegenerateImage 改为 async + gate + 依赖数组加 isComfyImage；runImageGeneration ComfyUI 成功分支加 consumeComfyuiPack 扣减；JSX 加 UserAccountModal + ConfirmDialog |
| `.docs/NyaaComfyui-package-plan/开发计划-SSOT.md` | P6 标记 ✅ |

## 仍需继续验证 / 已知问题

- **三条路径真机验证**（未登录/有余额生图并−1/无余额弹窗）需在 P7 rebuild 后进行
- 扣减是 fire-and-forget：网络波动导致扣减调用失败时，服务端已扣但前端未感知；下次 gate 或 profile 刷新会自动修正

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1-P6 ✅（后端+数据层+账号面板+节点UI+对话生图gate+扣减 全部完成并推送）。

请以 plan 模式推进 P7 — 端到端联调 + rebuild 真机验证：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT §6 全部 P）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-006.md（本文件）
3. P7 任务：rebuild NyaaChat（python rebuild.py）；跨 NyaaChat↔NyaaAcount 全链路验证
4. 验证路径：登录→节点启用→生图−1→耗尽→扩容扣猫粮+30→继续生图
5. DB 备份留存至确认无副作用；git status 双仓库干净
6. 收尾：双仓库 commit-push + SSOT 全部标记 ✅ + memory 记录 V1 完成
7. 遵循 NyaaChat CLAUDE.md 和 Vibo Coding 规范
```
