# NyaaComfyui图包 V1 阶段交接 004

## 交接目的

本文件记录 P4 阶段的完成状态，为 P5（ComfyUI 节点 UI）提供续接入口。

**续接前必读文档**：
- `NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md` — SSOT（P1-P4 ✅）
- `NyaaChat\.docs\NyaaComfyui-package-plan\设计审核与决策.md` — 设计决策 + 对标索引
- `NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-003.md` — P3 API 参考

## 当前进度

P1 ✅ → P2 ✅ → P3 ✅ → P4 ✅ **已完成**

P4 — 共享账号面板 UI 完成：
- `UserAccountModal.tsx` economy 区块新增「ComfyUI图包」行
- 显示：`剩余 {comfyuiPackRemaining} 次`（只显示剩余，不显示上限）
- 扩容按钮复用 `pendingExpand` / `ConfirmDialog` 模式
- 扩容 handler `expandComfyuiPack` 调 `apiExpandComfyuiPack(token)` → 刷新 profile → flash
- `npm run build` 通过
- commit `c8de47e` 已推送

## 本轮已修复/已实现

| 文件 | 改动 |
|------|------|
| `src/components/UserAccountModal.tsx` | 导入 Image/expandComfyuiPack + 常量 COMFY_PACK_COST/STEP + expandComfyuiPack handler + ComfyUI图包 Row |

## P5 开发参考

P5 需要在 `ImageProvidersModal.tsx` 的 `ComfyProviderDetail` 中：
- **连通性检查**区块后方加「图包剩余」显示 + 扩容按钮（逻辑同 P4 的 Row）
- **启用开关 gate**：`comfyui-fixed`/`comfyui-custom` 从关→开时检查登录态，未登录打开 `UserAccountModal`
- 读取 `profile.comfyuiPackRemaining`，未登录显示「登录后可见」
- 对标代码位置：`ImageProvidersModal.tsx:637-828`（ComfyProviderDetail）、`:401-403`（DetailHeader toggle）

## 仍需继续验证/已知问题

- 无需用户手动验证（P4 纯 UI 层，build 通过即可确认无误）
- P5 需要前端 rebuild 真机验证（涉及 ComfyUI 节点交互）

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1-P4 ✅（后端+数据层+账号面板 UI 全部完成并推送）。

请以 plan 模式推进 P5 — ComfyUI 节点 UI：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT §6.2）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-004.md（本文件）
3. P5 任务：
   a. ImageProvidersModal.tsx ComfyProviderDetail 连通性检查后加「图包剩余」+ 扩容按钮
   b. 启用开关 gate：comfyui-fixed/custom 关→开时检查登录态，未登录→打开 UserAccountModal
4. 读取 profile.comfyuiPackRemaining；未登录显示「登录后可见」
5. 注意计费边界：仅 comfyui-fixed 和 comfyui-custom 触发图包判定（用户自定义 comfyui 节点不计费）
6. 验证：npm run build 通过
7. 遵循 NyaaChat CLAUDE.md 和 Vibo Coding 规范
```
