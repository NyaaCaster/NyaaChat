# NyaaComfyui图包 V1 阶段交接 001

## 交接目的

本文件记录 P1 阶段的完成状态，为 P2（shared-server 后端）提供续接入口。

**续接前必读文档**：
- `NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md` — SSOT 计划（含全部 7 个 P 阶段）
- `NyaaChat\.docs\NyaaComfyui-package-plan\设计审核与决策.md` — 设计决策记录
- `NyaaChat\.docs\NyaaComfyui-package-plan\NyaaComfyui图包-初始设计.md` — 原始需求

## 当前进度

P1 — NyaaAcount 定价登记 ✅ **已完成**

- NyaaAcount `pricing.json` 和 `events.json` 已登记 `expand_comfyui_pack` action（5 猫粮 / +30 次）
- consume 端点验证通过（action 识别、amount_mismatch、幂等重放、退款补偿）
- NyaaAcount 已 rebuild + commit-push：`e736880 feat: add expand_comfyui_pack pricing for NyaaComfyui图包 (+30次/5猫粮)`
- 测试扣款已退款，用户 `nyaa`(uid=2) 余额保持 45

## 本轮已修复/已实现

| 文件 | 改动 |
|------|------|
| `NyaaAcount/api/src/topup/pricing.json` | `nyaachat` 段新增 `expand_comfyui_pack: { amount: 5, label: "ComfyUI图包 +30", effect: "ComfyUI图包剩余次数 +30" }` |
| `NyaaAcount/api/src/topup/events.json` | `consume.NyaaChat` 段新增 `expand_comfyui_pack: "扩容 ComfyUI图包（+30 次，5 猫粮）"` |

## 无需改动

- NyaaAcount `.env` — token/key 复用现有 `nyaachat` 项目凭证（`NYAAACOUNT_API_TOKEN_NYAACHAT`、`NYAAACOUNT_ENCRYPTION_KEY_NYAACHAT`）
- `project.js` / `db.js` / `auth.js` — consume 端点通用逻辑无变化

## 仍需继续验证/已知问题

- 无需用户验证（P1 仅平台侧登记，无前端 UI 变更）
- P2 开始前需确保 NyaaAcount 容器处于重建后状态（当前已是）

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1（NyaaAcount 定价登记）已完成并推送。

请以 plan 模式推进 P2 — shared-server 后端：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\设计审核与决策.md（对标索引）
3. P2 任务：shared-server/src/db.js ALTER TABLE 加列 + account.js 常量/语句/2端点/profileOf 字段
4. 改数据库前必须备份 .db 文件
5. 验证后必须清理测试数据
6. 遵循 NyaaChat 的 CLAUDE.md 和 Vibo Coding 规范
```
