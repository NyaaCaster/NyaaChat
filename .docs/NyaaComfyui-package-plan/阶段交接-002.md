# NyaaComfyui图包 V1 阶段交接 002

## 交接目的

本文件记录 P2 阶段的完成状态，为 P3（前端数据层）提供续接入口。

**续接前必读文档**：
- `NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md` — SSOT 计划（P1✅ P2✅）
- `NyaaChat\.docs\NyaaComfyui-package-plan\设计审核与决策.md` — 设计决策 + 对标索引
- `NyaaChat\.docs\NyaaComfyui-package-plan\NyaaComfyui图包-初始设计.md` — 原始需求（特别注意第 12 行计费边界）

## 当前进度

P1 ✅ → P2 ✅ **已完成**

P2 — shared-server 后端完成：
- DB 迁移：`users` 表新增 `comfyui_pack_remaining INTEGER NOT NULL DEFAULT 10`
- 三个常量：`COMFY_PACK_FREE=10`、`COMFY_PACK_EXPAND_STEP=30`、`COMFY_PACK_EXPAND_COST=5`
- 两个 prepared statements：`expandComfyuiPack`（+=30）、`consumeComfyuiPack`（原子 −1, WHERE remaining>0）
- `profileOf` 新增 `comfyuiPackRemaining` 字段
- 扩容端点 `POST /account/expand-comfyui-pack`（对标 expand-kb 四步模式，无硬顶）
- 消耗端点 `POST /account/consume-comfyui-pack`（纯本地，WHERE remaining>0 原子守卫，409 pack_exhausted）
- 10 项测试全部通过：profileOf 字段、扩容、消耗、耗尽 409、扩容恢复
- 测试后已恢复原始数据（nyaa 用户 comfyui_pack_remaining=65）
- commit `2bc06dd` 已推送 NyaaChat master

## 本轮已修复/已实现

| 文件 | 改动 |
|------|------|
| `shared-server/src/db.js` | ALTER TABLE 加 `comfyui_pack_remaining` 列（try/catch） |
| `shared-server/src/routes/account.js` | 常量 + expandComfyuiPack/consumeComfyuiPack 语句 + profileOf 字段 + 扩容端点 + 消耗端点 |

## 前端数据层 API 参考（P3 会用到的端点）

| 端点 | 方法 | 路径 | 响应关键字段 |
|------|------|------|-------------|
| 扩容 | POST | `/api/shared/account/expand-comfyui-pack` | `{ ok, profile: { comfyuiPackRemaining } }` |
| 消耗 | POST | `/api/shared/account/consume-comfyui-pack` | `{ ok, remaining }` 或 409 `{ error: "pack_exhausted" }` |
| 读取 | GET | `/api/shared/account/profile` | `{ ok, profile: { comfyuiPackRemaining } }` |

## 仍需继续验证/已知问题

- 无需用户手动验证（P2 无前端 UI 变更）
- P3 开始前需确认 shared-server 容器处于当前状态

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1✅ P2✅（后端 DB+端点+profileOf 已上线并推送）。

请以 plan 模式推进 P3 — 前端数据层：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-002.md（本文件，含 API 参考）
3. P3 任务：src/lib/sharedAccountApi.ts — AccountProfile 加 comfyuiPackRemaining + expandComfyuiPack(token) + consumeComfyuiPack(token) 两个 API 函数
4. 验证：tsc --noEmit + eslint 通过
5. 遵循 NyaaChat CLAUDE.md 和 Vibo Coding 规范
```
