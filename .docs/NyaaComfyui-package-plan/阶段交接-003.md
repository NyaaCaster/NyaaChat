# NyaaComfyui图包 V1 阶段交接 003

## 交接目的

本文件记录 P3 阶段的完成状态，为 P4（共享账号面板 UI）提供续接入口。

**续接前必读文档**：
- `NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md` — SSOT（P1✅ P2✅ P3✅）
- `NyaaChat\.docs\NyaaComfyui-package-plan\设计审核与决策.md` — 设计决策 + 对标索引
- `NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-002.md` — P2 API 参考

## 当前进度

P1 ✅ → P2 ✅ → P3 ✅ **已完成**

P3 — 前端数据层完成：
- `AccountProfile` 接口新增 `comfyuiPackRemaining: number` 字段
- 新增 `ConsumePackPayload` 接口（`{ ok: true; remaining: number }`）
- 新增 `expandComfyuiPack(token)` 函数（调 `/expand-comfyui-pack`，返回 `ProfilePayload`）
- 新增 `consumeComfyuiPack(token)` 函数（调 `/consume-comfyui-pack`，返回 `ConsumePackPayload`）
- tsc（无新增错误）+ eslint（无错误）通过
- commit `baa3fc9` 已推送 NyaaChat master

## 本轮已修复/已实现

| 文件 | 改动 |
|------|------|
| `src/lib/sharedAccountApi.ts` | AccountProfile +1 字段, ConsumePackPayload 接口, expandComfyuiPack, consumeComfyuiPack |

## P4 开发参考：前端可用的 API 函数

```ts
// 读取剩余（已在 profile 中，登录后自动获取）
profile.comfyuiPackRemaining  // number

// 扩容：扣 5 猫粮，+30 次，返回新 profile
import { expandComfyuiPack } from "../lib/sharedAccountApi";
const res = await expandComfyuiPack(token);
// res.data.profile.comfyuiPackRemaining → 新剩余值

// 消耗：−1 次，返回新 remaining（409 时弹扩容提示）
import { consumeComfyuiPack } from "../lib/sharedAccountApi";
const res = await consumeComfyuiPack(token);
if (res.kind === "ok") {
  // res.data.remaining → 新剩余值
} else if (res.kind === "error" && res.error === "pack_exhausted") {
  // 弹窗：剩余次数不足，是否扩容？
}
```

## 仍需继续验证/已知问题

- 无需用户手动验证（P3 纯数据层，无 UI 变更）
- P4 中前端 rebuild 会触发类型贯通的全量 tsc 检查

## 续接提示词

```
继续 NyaaComfyui图包 开发。当前进度：P1✅ P2✅ P3✅（数据层 API 函数已就绪并推送）。

请以 plan 模式推进 P4 — 共享账号面板 UI：
1. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\开发计划-SSOT.md（SSOT）
2. 读取 NyaaChat\.docs\NyaaComfyui-package-plan\阶段交接-003.md（本文件，含 P3 API 参考）
3. P4 任务：src/components/UserAccountModal.tsx — economy 区块加「ComfyUI图包」行（对标「知识库栈」行）+ 扩容按钮（复用 ConfirmDialog）
4. 验证：npm run build 通过
5. 注意：剩余次数只显示不显示上限，只显示"剩余 N 次"
6. 遵循 NyaaChat CLAUDE.md 和 Vibo Coding 规范
```
