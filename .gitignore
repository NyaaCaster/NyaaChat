node_modules/
build/
dist/
coverage/
.DS_Store
*.log
src/temp/
.env*
!.env.example
# Claude Code per-machine settings (permissions, env, etc.) — not for sharing
.claude/settings.local.json
# Playwright MCP browser artifacts
.playwright-mcp/
# Local reference material — not for sharing
.ref/
# Operator-managed third-party SillyTavern extensions. Keep only registry.json
# in this repo; extension project directories are added by git/rebuild outside
# NyaaChat source control.
public/extensions/*/
